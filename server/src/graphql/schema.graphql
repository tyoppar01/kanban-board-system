type Board {
  id: ID
  taskList: [Task!]!      
  columns: [Column!]!   
  order: [String!]!
}

type Column {
  id: String!
  taskIds: [Int!]!
}

type Task {
  id: Int!
  title: String!
  description: String
  createdDate: String
  modifiedDate: String
}

type Query {
  board: Board!
}